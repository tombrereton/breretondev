---
interface Props {
  url: string;
  text: string;
}

const { url, text } = Astro.props;
---

<a class="tb-link" href={url}>
  {text}
</a>

<style>
  .tb-link {
    @apply font-sans;
    @apply text-grey-light;
  }
</style>
